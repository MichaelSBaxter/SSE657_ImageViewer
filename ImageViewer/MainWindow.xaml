<Window x:Class="ImageViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ImageViewer"
        mc:Ignorable="d"
        Title="Image Viewer" Height="350" Width="525">
    <Window.Resources>
        <local:ImageManager x:Key="imageManager"/>
    </Window.Resources>
    <DockPanel x:Name="mainDock">
        <Menu DockPanel.Dock="Top" x:Name="menu" HorizontalAlignment="Left" Height="Auto" VerticalAlignment="Top" Width="{Binding ActualWidth, ElementName=mainDock}" Background="{DynamicResource {x:Static SystemColors.MenuBarBrushKey}}">
            <MenuItem Header="_File">
                <MenuItem Header="Open" Click="MenuItem_Open"/>
            </MenuItem>
        </Menu>
        <Grid x:Name="displayGrid" DockPanel.Dock="Bottom" Width="{Binding ActualWidth, ElementName=mainDock}" DataContext="{StaticResource ResourceKey=imageManager}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Rectangle Grid.Row="0" Grid.Column="0" Height="{Binding ActualHeight, ElementName=displayGrid}" Width="{Binding ActualWidth, ElementName=displayGrid}" Fill="Black" IsHitTestVisible="False"/>
            <Image x:Name="image" Grid.Row="0" Grid.Column="0" Height="{Binding CurrentImageHeight}" Width="{Binding CurrentImageWidth}"  Source="{Binding CurrentImage}"/>
        </Grid>
    </DockPanel>
</Window>
